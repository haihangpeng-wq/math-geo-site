# 网站内容管理指南

## 文件结构总览

```
geometry-website/                  # 本地文件夹名（Git 仓库名：math-geo-site）
├── docs/                          # 所有 Markdown 文档（MDX）
│   ├── analytic/                  # 解析几何模块（内容完整）
│   │   ├── L1/                    # 世界观
│   │   │   ├── intro.md
│   │   │   ├── s0-1-parse-vs-complex.md
│   │   │   ├── ...（共约30+篇）
│   │   │   └── summary.md
│   │   └── L2/                    # 方法论
│   │       ├── intro.md
│   │       ├── simplicity.md
│   │       └── ...（共5篇）
│   ├── complex/                   # 复数模块（建设中）
│   │   └── index.md
│   ├── trig/                      # 三角模块（建设中）
│   │   └── index.md
│   ├── trilinear/                 # 三线坐标模块（建设中）
│   │   └── index.md
│   ├── vector/                    # 向量模块（建设中）
│   │   └── index.md
│   └── overview.md                # 总纲文档（docs 系统内，侧边栏"总纲"链接指向 /overview 页面）
│
├── src/
│   └── pages/                     # 独立页面（React 组件，不绑定侧边栏）
│       ├── index.tsx              # 首页
│       └── overview.tsx           # 计算几何总纲页面（路由 /overview）
│
├── sidebars.ts                    # 侧边栏配置
├── docusaurus.config.ts           # 站点配置（导航栏、插件等）
└── package.json                   # 依赖声明
```

---

## 一、添加新文章

### 1. 创建 Markdown 文件

在对应模块的文件夹下新建 `.md` 文件，例如在解析 L2 下添加新文章：

**文件路径：** `docs/analytic/L2/new-topic.md`

```markdown
---
slug: /analytic/L2/new-topic
sidebar_position: 5
sidebar_label: §5 新话题       # 可选，不写则用 H1 标题
---

# §5 新话题

正文内容……
```

**文件名规则：**
- 用英文短横线命名（如 `shift-axis.md`）
- 建议带编号前缀以控制排序（如 `s0-1-xxx.md`、`simplicity.md`）

**Frontmatter 字段说明：**

| 字段 | 必须 | 说明 |
|------|------|------|
| `slug` | 是 | URL 路径，如 `/analytic/L2/new-topic` |
| `sidebar_position` | 推荐 | 侧边栏排序号 |
| `sidebar_label` | 否 | 侧边栏显示名称，默认用文件 H1 标题 |

### 2. 注册到侧边栏

编辑 `sidebars.ts`，在对应 sidebar 的 items 数组中添加：

```typescript
// sidebars.ts — analyticSidebar 的 L2 方法论部分
{
  type: 'category',
  label: '解析几何的方法论',
  collapsed: false,
  link: { type: 'doc', id: 'analytic/L2/intro' },
  items: [
    'analytic/L2/simplicity',
    'analytic/L2/shift-axis',
    'analytic/L2/overflow-variables',
    'analytic/L2/coordinate-flow',
    'analytic/L2/new-topic',       // ← 新增
  ],
},
```

字符串值 `'analytic/L2/new-topic'` 就是文件相对于 `docs/` 的路径（去掉 `.md`）。

---

## 二、修改现有文章

直接编辑对应的 `.md` 文件即可。常用操作：

| 操作 | 方法 |
|------|------|
| 修改标题 | 改 H1（`# 标题`）或设置 `sidebar_label` |
| 修改侧边栏排序 | 改 `sidebar_position` 数值 |
| 修改 URL | 改 `slug` |
| 修改侧边栏分组 | 移动文件到不同子文件夹，并更新 `sidebars.ts` |

---

## 三、添加新的方法模块（如「面积法」）

假设要新增一个「面积法」模块：

### 1. 创建文档

```
docs/area/
├── index.md        ← 模块首页（建设中占位或正式内容）
├── L1/
│   └── intro.md
└── L2/
    └── intro.md
```

> **命名规范**：已有模块（analytic 除外）均使用 `index.md` 作为模块首页，子章节放在 `L1/`、`L2/` 子目录下。新建模块请沿用此规范。

### 2. 配置侧边栏

在 `sidebars.ts` 中新增一个 sidebar：

```typescript
areaSidebar: [
  {
    type: 'link',
    href: '/overview',
    label: '计算几何总纲',
  },
  {
    type: 'category',
    label: '面积法的世界观',
    collapsed: false,
    link: { type: 'doc', id: 'area/L1-intro' },
    items: [],
  },
  {
    type: 'doc',
    id: 'area/L2-intro',
    label: '面积法的方法论',
  },
],
```

### 3. 添加导航栏入口

在 `docusaurus.config.ts` 的 navbar items 中添加：

```typescript
{
  type: 'doc',
  docId: 'area/L1-intro',
  sidebarId: 'areaSidebar',
  position: 'left',
  label: '面积',
},
```

---

## 四、首页和总纲页面

| 页面 | 文件 | 修改方式 |
|------|------|---------|
| 首页 | `src/pages/index.tsx` | 直接编辑 React 组件 |
| 计算几何总纲 | `src/pages/overview.tsx` | 直接编辑 React 组件 |

这两个页面在 `src/pages/` 下，是独立页面，**不通过 docs 系统管理**，不会出现在任何侧边栏中。

---

## 五、本地预览与发布

```bash
# 本地开发（热更新，改完自动刷新）
cd /Users/home/Code/geometry-website
npm run start

# 构建生产版本
npm run build

# 部署到 Cloudflare Pages（通过 GitHub 推送自动触发）
git add .
git commit -m "更新内容"
git push
```

---

## 六、Markdown 写作语法速查

```markdown
# 一级标题（页面标题，也是侧边栏默认标签）

## 二级标题

### 三级标题

**加粗**  *斜体*

- 无序列表项
- 无序列表项

1. 有序列表
2. 有序列表

> 引用块

| 表头1 | 表头2 |
|-------|-------|
| 内容  | 内容  |

行内公式：$E=mc^2$

独立公式：
$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$

[内部链接](/docs/analytic/L1/intro)
[外部链接](https://example.com)
```

---

## 七、MDX 写作注意事项（手动编辑必读）

Docusaurus 使用 MDX 格式，以下语法**会导致构建报错**，手动编辑时请务必避免：

### ❌ 禁止写法

| 写法 | 问题 | 正确替代 |
|------|------|----------|
| `<div style="text-align: center">` | MDX 不支持 HTML style 属性 | 用 admonition 或 CSS 类名 |
| `<span style="color:red">` | 同上 | 用 `**加粗**` 或自定义 CSS |
| 未闭合的 JSX 标签 | 构建直接报错 | 确保所有 `<tag>` 有 `</tag>` |
| `{` 或 `}` 裸露使用 | 被当作 JSX 表达式解析 | 用 `\{` `\}`，或改用纯 Markdown |

### ✅ 推荐写法

**信息框（admonition）：**
```markdown
:::info
普通提示信息
:::

:::tip
技巧提示
:::

:::warning
注意事项
:::

:::danger
严重警告
:::
```

**数学公式：**
```markdown
行内公式：$E=mc^2$（用单个 `$` 包裹）

独立公式（居中）：
$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$
（用双 `$$` 包裹，公式内换行用 `\\`）
```

**代码块：** 用三个反引号，注明语言：
````markdown
```python
def hello():
    print("Hello")
```
````

### 修改配置后预览无变化？

某些修改（如 `sidebars.ts`、导航栏）需要重启 dev server：
```bash
# Ctrl+C 停掉，然后：
lsof -ti:3000 | xargs kill -9   # 杀掉占用 3000 端口的进程
npm run start
```
