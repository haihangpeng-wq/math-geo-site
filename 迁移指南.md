# 数学网站项目迁移指南

## 1. 项目概况

| 项目 | 内容 |
|------|------|
| **框架** | Docusaurus 3.10.0 + TypeScript |
| **用途** | 平面几何竞赛计算方法学习站（解析、复数、三角、三线、向量五大模块） |
| **Git 仓库** | `https://github.com/haihangpeng-wq/math-geo-site.git`（main 分支） |
| **部署** | Cloudflare Pages，域名 `penghaihang-9plus2bythehouse-computationalgeometry-math.pages.dev` |
| **数学渲染** | KaTeX（`remark-math` + `rehype-katex`） |

## 2. 文件结构

```
geometry-website/
├── docusaurus.config.ts    ← 站点配置（标题、导航栏、插件、部署URL）
├── sidebars.ts             ← 侧边栏定义（analytic/complex/trig/trilinear/vector）
├── package.json            ← 依赖声明
├── package-lock.json       ← 锁定版本（有则保留，加速安装）
├── tsconfig.json           ← TS 配置
├── 内容管理指南.md           ← 内容添加操作说明
├── docs/                   ← 文档内容（MDX）
│   ├── analytic/           ← 解析几何（主要模块，有完整内容）
│   │   ├── L1/             ← 第一章：预备知识（坐标、直线等）
│   │   └── L2/             ← 第二章：进阶技巧（平移、换元等）
│   ├── complex/index.md    ← 复数（建设中）
│   ├── trig/index.md       ← 三角（建设中）
│   ├── trilinear/index.md  ← 三线（建设中）
│   ├── vector/index.md     ← 向量（建设中）
│   └── overview.md         ← 总览页
├── src/
│   ├── pages/              ← 自定义页面（首页 index.tsx、overview.tsx）
│   ├── components/         ← 组件
│   ├── css/                ← 样式（custom.css）
│   └── theme/              ← 主题覆盖
├── static/                 ← 静态资源（图片、favicon等）
└── blog/                   ← 博客目录（可选，目前为空）
```

## 3. 本地启动

```bash
cd /Users/home/Code/geometry-website
npm install
npm run start          # 本地预览，默认 localhost:3000
```

构建生产版本：
```bash
npm run build
npm run serve          # 本地预览构建产物
```

## 4. 部署

项目通过 Cloudflare Pages 部署，连接 GitHub 仓库自动构建。推送代码到 `main` 分支即触发部署。

如果需要本地手动部署到 Cloudflare：
```bash
npx wrangler pages deploy build --project-name=math-geo-site
```

## 5. 关键技术注意事项

### 导航栏内部链接
`docusaurus.config.ts` 中的 navbar items，内部页面**必须**用：
```ts
{ type: 'doc', docId: 'xxx', sidebarId: 'xxx', label: 'xxx' }
```
不能用 `type: 'link'`（仅适用于外部 URL）。

### MDX 语法限制
- **不能写 HTML style 属性**，如 `<div style="text-align: center;">` 会报错
- 用 `:::info`、`:::tip`、`:::warning` 等 admonition 代替样式化区块
- 数学公式用 `$...$`（行内）和 `$$...$$`（块级），无需额外配置

### 侧边栏编号
各章节标题带编号格式（如 `§0.1 解析 VS 复数`），在 md 文件 frontmatter 中设置：
```yaml
---
sidebar_label: §0.1 解析 VS 复数
---
```

### 修改配置后预览无变化
某些配置修改（如侧边栏结构）需要重启 dev server：
```bash
# 杀掉 3000 端口进程，然后重新启动
lsof -ti:3000 | xargs kill -9
npm run start
```

## 6. 添加新内容

### 为已有模块添加章节
1. 在 `docs/{module}/` 下创建 `.md` 文件
2. 文件头部设置 frontmatter（`sidebar_label`、`sidebar_position`）
3. 在 `sidebars.ts` 对应 sidebar 中添加条目（或使用 `autogenerated` 自动索引）

### 为"建设中"模块添加内容
1. 在 `docs/{module}/` 下创建子目录（如 `L1/`）
2. 添加章节 md 文件
3. 在 `sidebars.ts` 中扩展对应 sidebar，添加分类和条目
4. 更新 `docs/{module}/index.md` 的内容

## 7. 给新 AI 的上下文提示词

> 这是一个 Docusaurus 3 数学教学网站项目。技术栈：Docusaurus 3.10.0 + TypeScript + KaTeX（remark-math + rehype-katex）。GitHub 仓库：haihangpeng-wq/math-geo-site，部署于 Cloudflare Pages。
>
> 核心配置文件：
> - `docusaurus.config.ts`：站点配置、导航栏、插件
> - `sidebars.ts`：五个模块的侧边栏结构
>
> 内容结构：`docs/` 目录下五个子目录对应解析、复数、三角、三线、向量五大模块。解析几何模块内容完整（`docs/analytic/L1/` 和 `L2/`），其余四个模块目前只有建设中页面。
>
> 注意事项：
> - 导航栏内部链接用 `type: 'doc'`，不用 `type: 'link'`
> - MDX 中不写 HTML style，用 admonition
> - 侧边栏标签用 § 编号格式
> - 本地预览：`npm run start`
